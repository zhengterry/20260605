# 万能导入 V2 - 智能多格式批量下单系统

## 环境配置

1. 复制 `.env.example` 为 `.env.local`
2. 配置以下环境变量：

```
DEEPSEEK_API_KEY=你的DeepSeek API密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com
```

## 运行

```bash
npm install
npm run dev
```

访问 http://localhost:3000

## 部署

```bash
npm run build
```

部署到 Vercel 时，在 Vercel 项目中配置相同的环境变量。
